#!/bin/sh
# Strip Cursor co-author trailer from commit messages (silent, non-blocking).
MSG_FILE="$1"
if [ ! -f "$MSG_FILE" ]; then
  exit 0
fi

grep -v '^Co-authored-by: Cursor <cursoragent@cursor.com>[[:space:]]*$' "$MSG_FILE" > "$MSG_FILE.tmp" &&
  mv "$MSG_FILE.tmp" "$MSG_FILE"

exit 0
